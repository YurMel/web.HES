@model HES.Web.Pages.Employees.IndexModel

<form id="orgStructureForm" data-action="CreateDepartment" method="post">
    <div asp-validation-summary="ModelOnly" class="text-danger"></div>
    <input type="hidden" asp-for="Department.CompanyId" value="@ViewBag.CompanyId" />
    <div class="form-group mb-4">
        <label asp-for="Department.Name" class="control-label text-gray"></label>
        <input asp-for="Department.Name" class="form-control text-navyblue" />
        <span asp-validation-for="Department.Name" class="text-danger"></span>
    </div>
    <div class="form-group m-0 text-right">
        <button type="button" class="btn btn-secondary" data-dismiss="modal">Cancel</button>
        <input type="submit" value="Create" class="btn btn-primary" />
    </div>
</form>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}